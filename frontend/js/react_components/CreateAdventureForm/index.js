import React from 'react';
import ReactDOM from 'react-dom';

import CreateAdventureForm from './view';

export default (node) => {
    ReactDOM.render(
        <CreateAdventureForm />,
        node
    );
}
