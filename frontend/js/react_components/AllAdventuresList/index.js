import React from 'react';
import ReactDOM from 'react-dom';


import AllAdventuresList from 'view';


export default (node) => {
    ReactDOM.render(
        <AllAdventuresList />,
        node
    );
}
